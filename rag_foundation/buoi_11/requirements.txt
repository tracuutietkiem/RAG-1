# QUAN TRỌNG — CÁCH CÀI TORCH (đọc trước khi pip install), giống Buổi 10
#
#     pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# rồi mới pip install -r requirements.txt. Nếu máy đã cài torch bản CPU từ
# Buổi 10 trong .venv riêng của buổi đó thì buổi này cần cài lại vì mỗi buổi
# có .venv độc lập.

python-dotenv>=1.2,<2
sentence-transformers>=3.0,<4
neo4j>=5.24,<6
google-genai>=1.0,<2

# torch KHÔNG khai báo ở đây (xem hướng dẫn ở đầu file), lxml KHÔNG cần (buổi
# này không parse HTML).
